# V15 Formal Data Governance Increment Acceptance

Source archive: `E:\\临时文件夹\\MyNet_V15正式数据治理增量包_d6f9108.zip`

## Decision

**Conditionally accept the real-data governance changes; do not merge the
simulation exporter unchanged.**

The package correctly locks the measured line split, preserves the V15 label
geometry, and defines unambiguous trace-level supervision states.  It must not
be used to export a new simulation training set until its visible-phase label
selection is corrected.

## Verified

- The protected-hash registry contains 84 V15 NPZ archives and all 84 match the
  active V15 release byte-for-byte.
- The locked measured split is consistent throughout the staged artifacts:
  `LineL1`, `Line3`, and `Line7` train; `Line6` validation; `Line9` test;
  `LineX1` excluded.
- The staged formal-split validator passes and correctly reports zero confirmed
  measured negatives and `formal_training_allowed=false`.
- The staged data-contract check passes without claiming formal readiness.
- `17` focused tests pass after hydrating the incremental package with the
  unchanged repository modules and immutable V15 data.  Python compilation of
  all modified modules passes.

## Required correction before merge

`scripts/export_sim_training_npz.py` still resolves `y_soft_501x128.npy` before
`interface_mask_visible_phase_bscan.npy`.  The full simulation audit established
that the legacy `y_soft` mask is phase-misaligned for Batch 1/V1 cases.  The
exporter must select an explicitly declared canonical visible-phase label from
the simulation manifest and reject ambiguous legacy cases.

## Remaining project gates

- No confirmed negative measured trace/window exists.
- No non-Line9-conditioned, human-approved simulation case exists.
- The V15 real-data release therefore remains correctly frozen for formal
  training.
