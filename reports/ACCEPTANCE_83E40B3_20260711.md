# Non-paper Engineering Increment Acceptance

Source archive: `E:\\临时文件夹\\MyNet_d6f9108之后_非论文工程增量_Codex验收包_83e40b3.zip`

## Decision

**Conditionally accept the V2 simulation engineering framework.  Do not use it
for formal simulation training or claim physics validation yet.**

## Verified

- All 23 delivery checksums match and the included Git bundle verifies as a
  complete history at `83e40b3`.
- The V15 protected-invariant report preserves all 84 protected NPZ archives;
  Line9 remains test-only and LineX1 excluded.
- `compileall` passes.
- `scripts/validate_physical_sim_v2.py` passes for all four control cases.
- The declared engineering suite passes: `40 passed`.
- The gprMax runtime gate correctly fails in this environment because gprMax
  is not installed; it does not fabricate geometry or FDTD outputs.
- CTRL04 is correctly marked background-only and remains blocked from formal
  training pending actual solver output and visible-phase postprocessing.

## Required gates before simulation promotion

- Run geometry-only and FDTD controls in an isolated, audited gprMax 3.1.7
  environment; archive HDF5 output, CFL time step, receiver lengths, and the
  geometry views.
- Confirm the deployed antenna Tx/Rx geometry before treating the provisional
  0.18 m separation as physical evidence.
- Inspect PML reflections, polarity, and full-minus-no-basal visible-phase
  extraction, especially the 14 m moderate-loss control.
- Correct `scripts/export_sim_training_npz.py`: it still prioritizes legacy
  `y_soft_501x128.npy` over explicit visible-phase labels.  A manifest must
  declare one canonical label artifact and legacy ambiguous labels must be
  rejected.

## Scope boundary

This acceptance covers static contracts, controls, and gates only.  It does not
approve a new training dataset, negative class, formal physics claim, or paper
experiment.
