# Research Line Registry

| Status | Line | Use |
|---|---|---|
| Active | AeroPath-SSD | Acquisition-conditioned structured basal-interface path prediction; paper candidate after data gates pass. |
| Frozen baseline | ConvNeXt curve baseline | Required comparison baseline. |
| Frozen baseline | Route-2 GprMambaSep | Development comparison only; no physical A/S/G claim. |
| Review-only data | LineX1 | Never promotion-driving. |
| Test-only data | Line9 | Never condition simulation/training for strict evaluation. |
| Archived | Historical conversion scripts and V1 simulation pipelines | Reproducibility/reference only; do not use in new experiments. |

An asset is active only when it is referenced by an enabled configuration,
current contract, or the native V2 release workflow. Legacy scripts may retain
old drive paths as historical evidence, but they are not valid operational
entry points. New commands must use the portable runtime profile.
